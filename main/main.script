local monarch = require('monarch.monarch')

function init(self)
	msg.post('#', 'show_login_monarch')
end

function on_message(self, message_id, message, sender)
	if message_id == hash('show_login_monarch') then
		monarch.show('login_monarch')
	end
end